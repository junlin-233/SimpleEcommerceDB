# 简易电商平台管理系统 - 数据库设计说明书

## 1. 项目概述
本项目旨在开发一个基于 MySQL 8.0 和 Python 的简易电商平台后端系统。项目重点在于展示数据库原理的实际应用，包括关系模型设计、完整性约束、以及存储过程和触发器的高级应用。

## 2. E-R 关系模型设计
本系统包含四个核心实体，它们之间的关系如下：

### 实体列表
1.  **用户 (Users)**: 系统的参与者，分为管理员和普通用户。
2.  **商品 (Products)**: 平台售卖的货物。
3.  **订单 (Orders)**: 用户的一次购买行为记录（订单头）。
4.  **订单明细 (OrderDetails)**: 订单中具体的商品项。

### 实体关系 (ER图描述)
*   **Users - Orders (1:N)**: 一个用户可以创建多个订单，但一个订单只属于一个用户。
*   **Orders - Products (M:N)**: 一个订单包含多个商品，一个商品也可以被多个订单包含。
    *   *解决方案*: 通过引入**订单明细 (OrderDetails)** 关联表，将多对多关系拆解为两个一对多关系：
        *   Orders - OrderDetails (1:N)
        *   Products - OrderDetails (1:N)

## 3. 数据库原理应用说明

### 3.1 第三范式 (3NF) 遵循情况
*   **1NF (原子性)**: 所有字段都是不可再分的原子值（如地址未拆分，但在简单的用户表中已满足）。
*   **2NF (消除部分依赖)**: 表格都有主键。`OrderDetails` 表的主键是联合主键或是自增 ID，其非主属性（数量、单价）完全依赖于（订单+商品），不存在部分依赖。
*   **3NF (消除传递依赖)**:
    *   在 `OrderDetails` 表中，我们记录了 `unit_price`（下单时的单价）。虽然 `Products` 表也有 `price`，但商品价格是会变动的。这里记录 `unit_price` 是为了保留历史数据的真实性，**这不是冗余，而是业务必须的快照数据**。
    *   除此之外，表中不存在非主属性对主键的传递依赖。

### 3.2 存储过程 (Stored Procedures) 的业务痛点解决
在本项目中，我们拒绝在 Python 代码中直接拼接 SQL，而是使用存储过程，主要解决了以下痛点：
1.  **业务逻辑封装**: `sp_place_order` 将"检查价格"、"检查库存"、"创建订单"、"创建明细"这一系列动作封装在数据库层。Python 端只需要调用一个接口。
2.  **安全性**: 防止 SQL 注入攻击，且应用层不需要知道表结构的细节。
3.  **事务控制 (Transaction)**: 下单是一个原子操作。如果扣减库存失败，必须回滚订单创建。`sp_place_order` 内部使用了 `START TRANSACTION`, `COMMIT` 和 `ROLLBACK` 来确保数据的 ACID 特性。

### 3.3 触发器 (Triggers) 的自动化维护
我们实现了 `trg_after_order_detail_insert` 触发器：
*   **功能**: 当 `OrderDetails` 插入数据时，自动减少 `Products` 表的库存。
*   **解决痛点**:
    *   **防止超卖**: 配合 `Products` 表上的 `CHECK (stock_quantity >= 0)` 约束，如果触发器执行扣减后导致库存为负，数据库会报错并回滚整个事务。
    *   **数据强一致性**: 无论通过什么方式（Python, Navicat, 命令行）插入订单明细，库存扣减逻辑都会强制执行，不会因为程序员忘记写 Update 语句而导致库存错误。

### 3.4 视图 (Views)
*   `v_sales_report`: 屏蔽了复杂的多表连接 (JOIN) 逻辑，为上层应用提供一个简洁的"虚拟表"，方便管理员查看销售数据。

## 4. 索引优化 (Indexes)
为了提高查询性能，我们在以下字段建立了索引：
*   `Users.username`: 加速登录时的用户查找。
*   `Products.name`: 加速商品名称的模糊搜索。
*   `Orders.user_id`: 加速"我的订单"查询。

## 5. 快速开始
1.  在 MySQL 中新建数据库，运行 `init_ecommerce_db.sql`。
2.  修改 `main.py` 顶部的 `DB_CONFIG`，填入你的 MySQL 密码。
3.  运行 `python main.py` (命令行版) 或 `streamlit run streamlit_app.py` (网页版)。
4.  测试账号: `alice` / `pass123` (普通用户), `admin` / `admin123` (管理员)。
